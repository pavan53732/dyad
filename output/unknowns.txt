OUTSTANDING: Cross-boundary IPC call graph. The static parser identifies `invoke` strings and `handle` string registrations but linking them via static AST extraction guarantees less than 0.6 confidence without dynamic evaluation since some string channels may be computed. (Reason: Dynamic strings).
OUTSTANDING: React Router exact path-to-component mappings for dynamically generated nested routes (e.g. `$provider.tsx`). Confidence is medium since the filesystem structure dictates the route, but dynamic route loading behaviors might intercept them.
OUTSTANDING: Complex type derivations in Drizzle `schema.ts`. Drizzle types are statically defined, but the resulting relational mappings might have runtime constraints not visible without DB introspection.
UNKNOWN: Build-time side effects from the many Vite configurations (`vite.main.config.mts`, `vite.preload.config.mts`, etc.). Conflicting or conditional ENV variables injected at build time could not be perfectly replicated statically.